# 患者端订单功能实现总结

## ✅ 已完成功能

### 1. 订单生成逻辑 ✓
**实现位置**: `PaymentServiceImpl.createPayment()`

**计算逻辑**:
```java
// 1. 查询原始金额（ori_amount）- 从医生职称表获取挂号费
BigDecimal oriAmount = paymentMapper.getRegistrationFeeBySchedule(scheduleRecordId);

// 2. 查询报销类型 - 通过 patient.reimburse_id 关联 reimburse_type 表
ReimburseType reimburseType = paymentMapper.getReimburseTypeByPatient(patientId);

// 3. 计算实付金额（ask_pay_amount）
// 公式: 实付 = 原价 × (1 - 报销比例/100)
BigDecimal askPayAmount = oriAmount.multiply(
    BigDecimal.ONE.subtract(
        reimburseType.getPercent().divide(BigDecimal.valueOf(100), 4, RoundingMode.HALF_UP)
    )
).setScale(2, RoundingMode.HALF_UP);
```

**数据流**:
```
patient.reimburse_id -> reimburse_type.id -> reimburse_type.percent
doctor.doc_title_id -> title_number_source.id -> title_number_source.ori_cost
计算: ask_pay_amount = ori_cost × (1 - percent/100)
```

---

### 2. 订单列表接口 ✓
**接口**: `GET /api/payments?patientId={patientId}`

**实现**: `PaymentController.getPayments()`

**功能**:
- ✅ 返回患者所有订单
- ✅ 状态直接对应 `pay_record.pay_status` 字段（待支付/已支付/已取消）
- ✅ 包含完整订单信息：原价、实付、报销比例、医生、科室等
- ✅ 按时间倒序排列

**数据库查询**: 联表查询 `pay_record`, `doctor`, `user`, `department`, `patient`, `reimburse_type`

---

### 3. 订单支付接口 ✓
**接口**: `POST /api/payments/{paymentId}/pay`

**实现**: `PaymentServiceImpl.payOrder()`

**功能**:
- ✅ 简单模拟支付流程
- ✅ 验证医保余额充足性
- ✅ 扣除患者医保余额
  - 通过 `patient.medical_insuranceid` 关联 `medical_insurance` 表
  - 执行 `UPDATE medical_insurance SET overage = overage - ask_pay_amount`
- ✅ 更新订单状态为"已支付"
- ✅ 更新挂号记录状态为"已挂号"
- ✅ 记录支付时间

**事务保护**: 使用 `@Transactional` 确保操作原子性

---

### 4. 订单查看接口 ✓
**接口**: `GET /api/payments/{paymentId}`

**实现**: `PaymentServiceImpl.getPaymentById()`

**功能**:
- ✅ 返回指定订单详情
- ✅ 包含原始金额（`oriAmount`）
- ✅ 包含减免比例（`reimbursePercent`）
- ✅ 包含减免后金额（`askPayAmount`）
- ✅ **所有状态订单均返回 `pay_time` 作为时间**
  - 待支付: 创建时间
  - 已支付: 支付时间
  - 已取消: 创建时间

---

### 5. 退订订单接口 ✓
**接口**: `DELETE /api/payments/{paymentId}`

**实现**: `PaymentServiceImpl.cancelOrder()`

**功能**:
- ✅ 根据 `pay_record.id` 查找记录
- ✅ 设置 `pay_status` 为 '已取消'
- ✅ 根据 `patient_id` 和 `sch_id` 在 `register_record` 表索引记录
- ✅ 修改对应 `register_record.status` 为 '已取消'
- ✅ 如订单原状态为"已支付"，回补号源（`left_source_count + 1`）

---

## 📁 新增文件清单

### Java实体类
1. `PayRecord.java` - 支付记录实体
2. `MedicalInsurance.java` - 医保账户实体
3. `ReimburseType.java` - 报销类型实体

### DTO类
1. `PaymentDto.java` - 订单响应DTO
2. `CreatePaymentRequest.java` - 创建订单请求DTO

### Mapper接口
1. `PaymentMapper.java` - 支付相关数据访问接口
2. `PaymentMapper.xml` - MyBatis映射配置

### Service层
1. `PaymentService.java` - 支付服务接口
2. `PaymentServiceImpl.java` - 支付服务实现

### Controller层
1. `PaymentController.java` - 订单管理控制器

### 数据库脚本
1. `add_sch_id_to_pay_record.sql` - 为pay_record表添加sch_id字段

### 文档
1. `患者端订单API文档.md` - 完整API接口文档

---

## 🔧 修改文件清单

### Mapper接口增强
- `RegistrationMapper.java`: 新增 `updateRegistrationStatusToConfirmed()` 方法
- `RegistrationMapper.xml`: 新增挂号状态更新为"已挂号"的SQL

---

## 🗄️ 数据库变更

### 需执行的SQL脚本
```sql
-- 执行文件: add_sch_id_to_pay_record.sql

-- 1. 添加 sch_id 字段
ALTER TABLE pay_record ADD COLUMN sch_id VARCHAR(20);

-- 2. 添加外键约束
ALTER TABLE pay_record
ADD CONSTRAINT FK_PAY_RECORD_SCHEDULE
FOREIGN KEY (sch_id) REFERENCES doc_schedule_record (id)
ON DELETE SET NULL ON UPDATE CASCADE;

-- 3. 添加索引
CREATE INDEX idx_pay_schedule ON pay_record(sch_id);

-- 4. 添加注释
COMMENT ON COLUMN pay_record.sch_id IS '关联的排班记录ID';
```

---

## 🔄 完整业务流程

### 正常流程
```
1. 用户查看号源 (GET /api/registration/doctors)
   ↓
2. 用户发起挂号 (POST /api/registrations)
   → 扣减号源
   → 创建挂号记录（状态=待支付）
   ↓
3. 系统/用户创建支付订单 (POST /api/payments)
   → 计算实付金额（根据报销比例）
   → 创建支付记录（状态=待支付）
   ↓
4. 用户查看订单列表/详情
   (GET /api/payments 或 GET /api/payments/{id})
   → 显示原价、报销比例、实付金额
   ↓
5. 用户支付订单 (POST /api/payments/{id}/pay)
   → 验证医保余额
   → 扣减医保余额
   → 更新订单状态（待支付→已支付）
   → 更新挂号状态（待支付→已挂号）
   ↓
6. 用户就诊...
```

### 取消流程
```
用户取消订单 (DELETE /api/payments/{id})
   ↓
1. 更新订单状态 → 已取消
2. 更新挂号状态 → 已取消
3. 如订单已支付 → 回补号源
```

---

## 📊 数据库表关系

```
patient
├─ reimburse_id → reimburse_type.id (获取报销比例)
└─ medical_insuranceid → medical_insurance.id (医保余额)

pay_record
├─ patient_id → patient.id
├─ doc_id → doctor.id
└─ sch_id → doc_schedule_record.id (新增字段)

register_record
├─ patient_id, sch_id (联合主键)
└─ 与 pay_record 通过 (patient_id, sch_id) 关联

doc_schedule_record
└─ doc_id → doctor.id

doctor
└─ doc_title_id → title_number_source.id (获取挂号费ori_cost)
```

---

## 🎯 关键实现点

### 1. 订单金额计算准确性
- ✅ 从医生职称表获取原价
- ✅ 从报销类型表获取报销比例
- ✅ 使用 `BigDecimal` 进行精确计算，避免浮点误差
- ✅ 结果保留2位小数

### 2. 医保余额扣减安全性
- ✅ 原子操作：`UPDATE ... WHERE overage >= amount`
- ✅ 乐观锁机制，防止超扣
- ✅ 返回影响行数验证

### 3. 订单状态一致性
- ✅ 使用 `@Transactional` 确保事务
- ✅ 订单状态与挂号状态同步更新
- ✅ 失败自动回滚

### 4. 号源管理正确性
- ✅ 取消已支付订单时回补号源
- ✅ 取消待支付订单不回补（因挂号时已扣减）

---

## 🧪 测试建议

### 1. 功能测试
```bash
# 1. 创建订单
POST /api/payments
Body: {"patientId": "PAT0001", "scheduleRecordId": "SCH001"}

# 2. 查询订单列表
GET /api/payments?patientId=PAT0001

# 3. 查询订单详情
GET /api/payments/{paymentId}

# 4. 支付订单
POST /api/payments/{paymentId}/pay

# 5. 取消订单
DELETE /api/payments/{paymentId}
```

### 2. 边界测试
- [ ] 医保余额不足时支付
- [ ] 重复支付同一订单
- [ ] 取消已取消的订单
- [ ] 无报销类型的患者（全额支付）
- [ ] 并发扣减医保余额

### 3. 数据验证
- [ ] 验证 `ask_pay_amount` 计算正确性
- [ ] 验证医保余额扣减准确性
- [ ] 验证订单状态与挂号状态一致性
- [ ] 验证号源回补逻辑

---

## 🔍 前后端对接检查点

### 前端需要的字段（已全部提供）
- ✅ `paymentId` - 订单ID
- ✅ `payStatus` - 订单状态（待支付/已支付/已取消）
- ✅ `oriAmount` - 原价
- ✅ `askPayAmount` - 实付金额
- ✅ `reimbursePercent` - 报销比例
- ✅ `reimburseType` - 报销类型名称
- ✅ `payTime` - 订单时间（所有状态均返回）
- ✅ `doctorName` - 医生姓名
- ✅ `departmentName` - 科室名称

### 前端需要的接口（已全部实现）
- ✅ 创建订单
- ✅ 订单列表
- ✅ 订单详情
- ✅ 订单支付
- ✅ 取消订单

---

## ⚠️ 注意事项

1. **必须先执行数据库迁移脚本** `add_sch_id_to_pay_record.sql`
2. 确保 `patient` 表中每个患者都关联了 `reimburse_id` 和 `medical_insuranceid`
3. 确保 `medical_insurance` 表中有足够的余额进行测试
4. 支付操作不可逆，已支付订单只能取消而不能退款（业务规则）
5. 订单的 `payTime` 字段在所有状态下都有值，便于前端统一处理

---

## 📝 后续优化建议

1. **支付方式扩展**: 目前仅支持医保支付，可扩展支持微信、支付宝等
2. **退款功能**: 支持已支付订单退款并返还医保余额
3. **订单超时**: 待支付订单超时自动取消
4. **支付通知**: 支付成功后发送消息通知
5. **账单明细**: 提供月度/年度账单汇总
6. **发票功能**: 支持电子发票开具
