# ??????? SSH ??????????????????
patient.sshTunnel.enabled=false

# ???????????? redis??? redis repository??? redisKeyValueAdapter ?????
spring.data.redis.repositories.enabled=false

# ???????????????? MessageTaskService ?? @ConditionalOnProperty???????
patient.message.task.enabled=false

# ???????????????
patient.payment.timeoutScanDelayMs=600000

# ??????? PostgreSQL LISTEN ?????????? H2?
patient.message.pgListener.enabled=false

# Disable payment timeout scan during tests
patient.payment.timeoutScanEnabled=false

# Email verification: never send real emails in tests
patient.emailVerification.realSendEnabled=false
