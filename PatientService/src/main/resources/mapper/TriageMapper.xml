<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.Mapper.TriageMapper">

    <select id="fetchHistory" resultType="com.example.pojo.dto.TriageResponse">
        SELECT * FROM triage_record WHERE patient_id = #{patientId}
    </select>

    <insert id="generateSuggestions">
        INSERT INTO triage_record (patient_id, symptoms, recommendations, suggested_actions, created_at)
        VALUES (#{request.patientId}, #{request.symptoms}, #{request.recommendations}, #{request.suggestedActions}, NOW())
    </insert>

</mapper>
