<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.Mapper.FeedbackMapper">

    <insert id="insertFeedback">
        INSERT INTO registration_feedback (registration_id, score, tags, comment, created_time)
        VALUES (#{registrationId}, #{request.score}, #{request.tags}, #{request.comment}, NOW())
    </insert>

    <select id="fetchFeedback" resultType="com.example.pojo.dto.FeedbackResponse">
        SELECT * FROM registration_feedback WHERE registration_id = #{registrationId}
    </select>

</mapper>
